# Development profile. DB connection details come from DBProperties
# (db.properties + environment variables: DB_HOST, DB_PORT, DB_NAME,
# DB_USERNAME, DB_PASSWORD) via the Spring-managed HikariCP pool in
# DataSourceConfig.
app:
  upload-dir: ${APP_UPLOAD_DIR:./uploads}
