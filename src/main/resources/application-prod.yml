# Production profile. DB connection details come from DBProperties
# (environment variables: DB_HOST, DB_PORT, DB_NAME, DB_USERNAME, DB_PASSWORD)
# via the Spring-managed HikariCP pool in DataSourceConfig. All secrets MUST
# come from environment variables.
app:
  upload-dir: ${APP_UPLOAD_DIR:?set APP_UPLOAD_DIR to a persistent volume path}
