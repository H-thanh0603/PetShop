# Database connection (non-secret settings).
# The password MUST come from secrets.properties (gitignored) or the
# PETSHOP_DB_PASSWORD / DB_PASSWORD environment variable. Never commit it.
db.host = localhost
db.port = 3306
db.username = petshop
db.password =
db.dbname = petvaccine
db.option = useUnicode=true&characterEncoding=utf-8
